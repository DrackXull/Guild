rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: All data is readable and writable by any authenticated user.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
